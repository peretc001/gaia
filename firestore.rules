rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешаем чтение и запись для всех в режиме разработки
    // ВНИМАНИЕ: Это небезопасно для production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

